# Rastery

一款跨平台原生桌面图像工具箱，聚焦本地图片处理。本文件只定义领域词汇；架构与 crate 划分见 `docs/spec/design.md`，范围决策见 `docs/adr/`。

## Language

**板块**：
主界面的顶层导航分组，spec 定义了四个：基础图片处理、AI 生成与改图、行业定制 AI 工具、创作输出。这是**用户怎么找到功能**的组织方式，与功能是否需要联网无关。
_Avoid_: 模块、分类、tab

**本地功能**：
完全离线可用、不需要 API Key、不发出任何网络请求的功能（FR-01~FR-10）。图片进、图片出的确定性变换，可由 property test 与编译器全自动验证。构成 v1 的全部内容。
_Avoid_: 基础功能（spec §2.1 的「基础图片处理」只是其中一部分，不含创作输出）、离线功能

**AI 功能**：
需要联网、需要用户自备 API Key、验收依赖人眼主观判断的功能（FR-11、FR-20~FR-23、FR-30~FR-42）。推后至 v2，见 [ADR-0001](./docs/adr/0001-v1-scope-local-only.md)。
_Avoid_: 在线功能、云功能

> **本地功能 / AI 功能与板块是正交的两根轴。** 「创作输出」板块里 FR-10 GIF 是本地功能、FR-11 海报是 AI 功能；「基础图片处理」板块则整个都是本地功能。因此 v1 交付的不是「四个板块里的前两个」，而是横切所有板块的本地功能集合——结果是 v1 的主界面上，「AI 生成与改图」与「行业定制 AI 工具」两个板块是空的。

**本地优先**：
§1.3 的设计原则：基础功能完全离线可用且免费，不依赖网络与 AI。这是一条**产品承诺**，描述软件对用户的保证；它不是范围划分的名字——那个用「本地功能」。
_Avoid_: 用「本地优先」指代 v1 范围

**档位**：
行业定制 AI 工具中，用户唯一需要做的选择：一组预置的、语义化的参数选项（如证件照的「一寸/二寸」、头像工坊的「日漫风/赛博朋克」）。档位背后的提示词对用户不可见。「选档位 → 点生成」两步操作是 §1.3 原则 3「零提示词门槛」的落地形式。属 AI 功能，v2。
_Avoid_: 参数、选项、预设（「预设」在 spec 中另指 FR-01 的裁剪比例预设与 FR-23 的场景预设）

**保持不变项**：
行业定制 AI 工具的验收标准中，要求 AI 改图后必须原样保留的部分（如 FR-35 模特试穿的「服装款式/颜色/材质」、FR-41 菜品图美化的「菜品本身、分量、摆盘」）。只能由人眼主观判断，无法自动化——这是 [ADR-0001](./docs/adr/0001-v1-scope-local-only.md) 把 AI 功能整体推后的核心理由。
_Avoid_: 约束、不变量（后者在 requirements.md Requirement 54–58 中另指可自动验证的 property）
